import turtle

t = turtle.Turtle()
t.speed(0)
t.hideturtle()
turtle.bgcolor("black")
colors = ["red", "orange", "yellow", "green"]

for i in range(36):
    t.color(colors[i % 4])
    t.left(10)
    for j in range(4):
        t.circle(100, 90)
        t.left(90)

turtle.done()
